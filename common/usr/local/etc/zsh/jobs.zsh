#!/usr/bin/env zsh
# /usr/local/etc/zsh/jobs.zsh ~/.config/zsh/jobs.zsh

setopt check_jobs # upon exit
setopt hup # upon exit
setopt monitor # jobs
setopt no_notify
setopt posix_jobs # not in subshells

# EOF
