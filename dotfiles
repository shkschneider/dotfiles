#!/bin/sh

rsync \
    --exclude ".git" \
    --exclude "[a-z]*" \
    --exclude "[A-Z]*" \
    -Wav --human-readable --progress "." "$HOME"

rsync \
    --exclude ".*" \
    --exclude "dotfiles" \
    -Wav --human-readable --progress "." "/"

# EOF
