# @author shkschneider
# $ZSH_CUSTOM/functions/clean

find "${1:-.}" \( -name "*~" -print \
     -or -name ".*~" -print \
     -or -name "#*" -print \
     -or -name ".#*" -print \\
) -exec echo rm -rf "{}" \;

# EOF
